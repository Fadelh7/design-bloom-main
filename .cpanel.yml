---
deployment:
  tasks:
    - /bin/bash -lc 'if [ -x "$HOME/nodevenv/repositories/design-bloom-main/20/bin/npm" ]; then "$HOME/nodevenv/repositories/design-bloom-main/20/bin/npm" run cpanel:deploy; else /usr/bin/env npm run cpanel:deploy; fi'
